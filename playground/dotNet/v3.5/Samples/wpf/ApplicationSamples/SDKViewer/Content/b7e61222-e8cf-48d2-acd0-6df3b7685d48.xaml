<!--
/////////////////////////////////////////////////////////////////////////////
//
// Copyright (C) Microsoft Corporation, 2006.  All rights reserved.
//
/////////////////////////////////////////////////////////////////////////////
-->


<cc:SdkSinglePageViewer
  Name    ="_b7e61222_e8cf_48d2_acd0_6df3b7685d48"
  Tag     ="x:Name Attribute"
  xmlns:cc="clr-namespace:Microsoft.Windows.SdkViewer.Controls;assembly=SdkViewer"
  xmlns   ="http://schemas.microsoft.com/winfx/2006/xaml/presentation">
  <FlowDocument xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation">
    <Table CellSpacing="5">
      <TableRowGroup>
        <TableRow>
          <TableCell>
            <Paragraph FontSize="22"><Run>x:Name Attribute</Run>

            </Paragraph>

          </TableCell>

        </TableRow>

      </TableRowGroup>

    </Table>

    <Paragraph>Uniquely identifies object elements for purpose of programmatic access. An established <Run FontWeight="Bold">x:Name</Run>
 can be considered equivalent to the variable holding an object reference as returned by a constructor.
    </Paragraph>

    <Paragraph FontSize="22"><Run>Remarks</Run>

    </Paragraph>

    <Paragraph>The specified <Run FontWeight="Bold">x:Name</Run>
 becomes the name of a field that is created in the underlying code when XAML is parsed or compiled, and that field holds a reference to the object.
    </Paragraph>

    <Paragraph><Run FontWeight="Bold">x:Name</Run>
 must be unique within a name scope. In most common XAML cases, the namescope is defined by those elements contained in a single XAML page.
    </Paragraph>

    <Paragraph><Run FontWeight="Bold">x:Name</Run>
 cannot be applied in certain scopes. For instance, items in a ResourceDictionary cannot have names, because they already have the <Hyperlink NavigateUri="1985cd45-f197-42d5-b75e-886add64b248.xaml">x:Key Attribute</Hyperlink>
 as their unique identifier.
    </Paragraph>

    <Paragraph>Rules for permittance of <Run FontWeight="Bold">x:Name</Run>
 as well as the uniqueness scoping are defined by the underlying Windows Presentation Foundation (WPF) framework implementation that divides various markup elements into separate NameScope ranges, such as resource dictionaries, the logical element tree, etc.
    </Paragraph>

    <Paragraph>Some WPF framework applications may be able to avoid any use of the <Run FontWeight="Bold">x:Name</Run>
 attribute, because the Name dependency property as specified within the WPF namespace for several of the important base classes such as FrameworkElement/FrameworkContentElement satisfies this same purpose. There are still some common XAML and framework scenarios where programmatic access to an element with no <Run FontWeight="Bold">Name</Run>
 property exist, most notably in certain graphics support classes. For instance, you should specify <Run FontWeight="Bold">x:Name</Run>
 on timelines and transforms created in XAML, if you intend to reference them from procedural code.
    </Paragraph>

    <Paragraph>If <Run FontWeight="Bold">Name</Run>
 is defined as a property on an element, <Run FontWeight="Bold">Name</Run>
 and <Run FontWeight="Bold">x:Name</Run>
 can be used interchangeably, but an error will result if both are specified on the same element.
    </Paragraph>

    <Paragraph>For custom elements, the property (can be either a CLR property or <Run>dependency property</Run>
) that maps to <Run FontWeight="Bold">x:Name</Run>
 on any given object can be established or changed by designating that property with the RuntimeNamePropertyAttribute in the property declaration code.
    </Paragraph>

    <Paragraph><Run FontWeight="Bold">Name</Run>
 can be set using XAML attribute syntax, using XAML property element syntax, and in procedural code using SetValue; 
note however that setting the <Run FontWeight="Bold">Name</Run>
 property programmatically does not create the representative field reference within the namescope in some circumstances. . <Run FontWeight="Bold">x:Name</Run>
 cannot be set in XAML property element syntax, or in procedural code using <Run FontWeight="Bold">SetValue</Run>
; 
it can only be set using attribute syntax on elements.
    </Paragraph>

    <Paragraph FontSize="22"><Run>Example Attribute Usage</Run>

    </Paragraph>

    <Paragraph>
&lt;<Run FontStyle="italic">object</Run>
<Run FontWeight="Bold">x:Name</Run>
="<Run FontStyle="italic">aStringNameValue</Run>
".../>
    </Paragraph>

    <Paragraph FontSize="22"><Run>See Also</Run>

    </Paragraph>

    <Paragraph FontSize="16pt">Reference
    </Paragraph>

    <Paragraph>Name
      <LineBreak/>Name
      <LineBreak/>
    </Paragraph>

    <Paragraph FontSize="16pt">Concepts
    </Paragraph>

    <Paragraph><Hyperlink NavigateUri="e83f25e5-d66b-4fc7-92d2-50130c9a6649.xaml">Element Tree</Hyperlink>

      <LineBreak/>
      <LineBreak/>
    </Paragraph>

    <Paragraph/>
    <Paragraph/>
    <Paragraph><Span><Hyperlink NavigateUri="mailto:wsdkfdb@microsoft.com?subject=Feedback">Send comments</Hyperlink>
 about x:Name Attribute to Microsoft. </Span>

    </Paragraph>

  </FlowDocument>

</cc:SdkSinglePageViewer>

